# Set in stage2:
# ---------------------------------------------------------
# set -e +a +m +s +i -f
# isFirstRun
# VAR_*
# All functions in /start/functions

if [ "$isFirstRun" == "true" ]
then
   configFromVarGroup angel noEqual > "$VAR_CONFIG_DIR/angel.conf"
   configFromVarGroup param noEqual > "$VAR_CONFIG_DIR/lighttpd.conf"
fi
