# Set in stage2:
# ---------------------------------------------------------
# set -ef +am
# isFirstRun
# VAR_*
# All functions in /start/functions

if [ "$isFirstRun" == "true" ]
then
   if [ ! -s "$VAR_CONFIG_DIR/angel.conf" ]
   then
      createAngelConf
   fi
   if [ ! -s "$VAR_CONFIG_DIR/lighttpd.conf" ]
   then
      createLighttpdConf
   fi
fi
